{{!Creamos un contenedor}}
<div class="container p-4">
    {{!Creamos una fila para cada contenido}}
    <div class="row">
        {{!Creamos una columna de 4 unidades}}
        <div class="col-md-4 mx-auto">
            {{!Creamos una tarjeta}}
            <div class="card ">
                {{!Cuerpo de la tarjeta}}
                <div class="card-body">
                    <div class="card-title text-center">
                        <h3>
                            ACTUALIZAR SOFTWARE BASE
                            <br>
                            ({{sof_base.swb_nombre}})
                        </h3>
                    </div>
                    {{!Espacio SALTO DE LINEA}}
                    <br>
                    {{!Ruta del formulario y metodo a usar}}
                    <form action="/softwares/edit/{{sof_base.swb_id}}" method="POST">
                        {{!Campo para el nombre del programa}}
                        <div class="form-group">
                            <input type="text" value="{{sof_base.swb_nombre}}" name="swb_nombre" class="form-control"
                                placeholder="Programa..." autofocus>
                        </div>
                        {{!Espacio SALTO DE LINEA}}
                        <br>
                        {{!Campo para la version del programa}}
                        <div class="form-group">
                            <input type="text" value="{{sof_base.swb_version}}" name="swb_version" class="form-control"
                                placeholder="Version..." autofocus>
                        </div>
                        {{!Espacio SALTO DE LINEA}}
                        <br>
                        {{!Campo para la descripcion del programa}}
                        <textarea name="swb_detalle" rows="2" class="form-control"
                            placeholder="Descripcion...">{{sof_base.swb_detalle}}</textarea>
                        {{!Espacio SALTO DE LINEA}}
                        <br>
                        {{!Campo para la imagen del programa}}
                        <div class="form-group">
                            <input type="url" name="swb_imagen" value="{{sof_base.swb_imagen}}" class="form-control"
                                placeholder="URL de la imagen...">
                        </div>
                        {{!Espacio SALTO DE LINEA}}
                        <br>
                        {{!Campo para la URL de descarga del programa}}
                        <div class="form-group">
                            <input type="url" name="swb_descarga" value="{{sof_base.swb_descarga}}" class="form-control"
                                placeholder="URL de descarga...">
                        </div>
                        {{!Espacio SALTO DE LINEA}}
                        <br>

                        {{!Espacio SALTO DE LINEA}}
                        <br>
                        {{!Boton de guardar}}
                        <div class="form-group">
                            {{!Boton para guardar...}}
                            <button class="btn btn-success btn-block">
                                ACTUALIZAR
                            </button>
                            {{!Boton para cancelar...}}
                            <a href="/softwares" class="btn btn-secondary btn-block">
                                CANCELAR
                            </a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>